artifact.groupId = ${project.groupId}
artifact.id = ${project.artifactId}
artifact.version = ${project.version}

build.number = ${build.number}
build.timestamp = ${build.timestamp}
build.url= ${build.url}

build.java.vm.name = ${java.vm.name}
build.java.version = ${java.version}

build.os.arch = ${os.arch}
build.os.name = ${os.name}
build.os.version = ${os.version}

build.git.revision = ${build.git.revision}

minimum.methode.api.version = ${methode.api.version}

#buildInfo.mavenVersion = ${maven.version}
#buildInfo.gitTags =
